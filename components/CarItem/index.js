import React from 'react';
import {View, Text} from 'react-native';

const CarItem = (props) => {
    return (
        <View>
            <Text>test</Text>
        </View>
    );
};

export default CarItem;